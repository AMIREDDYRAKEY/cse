{
  "name": "cse-website-root",
  "private": true,
  "scripts": {
    "start": "cd server && npm start",
    "build": "npm install --prefix server && npm install --prefix cse && npm run build --prefix cse"
  }
}

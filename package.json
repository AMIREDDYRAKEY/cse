{
  "name": "cse-website-root",
  "private": true,
  "scripts": {
    "start": "node server/src/index.js",
    "build": "npm install && cd cse && npm install && npm run build && cd ../server && npm install"
  }
}